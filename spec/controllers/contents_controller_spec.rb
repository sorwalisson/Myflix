require 'rails_helper'


#RSpec.describe Api::V1::ContentsController, type: :controller do
  #describe 'verify if the controller is working' do
    #it 'verify #get if it is returning a JSON object' do
      #tester = create(:title)
      #ctester = create(:content, title_id: tester.id)
      #get
      #json = JSON.parse(response.body)
      #expect(json).to_not be_nil

